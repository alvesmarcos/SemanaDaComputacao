import React from 'react';
import {
  View,
} from 'react-native';
import Routes from '../navigation/Routes';

const Init = () => (
  <View style={{ flex: 1 }}>
    <Routes />
  </View>
);

export default Init;

