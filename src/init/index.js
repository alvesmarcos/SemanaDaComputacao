import React from 'react';
import {
  View,
} from 'react-native';
import Routes from '../navigation/Routes';
import { app as firebaseApp } from '../config';

const Init = () => <Routes />;

export default Init;
